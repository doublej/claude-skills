---
name: readable-ui
description: "Typography, spacing, contrast, labels, and copy for readable interfaces; use when designing or reviewing UI text, forms, dashboards, onboarding, or long-form content."
---

# Readable UI

## Overview

Use this skill when interface clarity depends on typography, spacing, contrast, hierarchy, or copy. It turns layout and text decisions into practical defaults for product UI, forms, dashboards, and reading-heavy surfaces.

## What To Optimize First

1. Legibility: size, weight, contrast, and line height.
2. Structure: headings, labels, grouping, and spacing.
3. Scannability: front-loaded words, short blocks, and obvious actions.
4. Error recovery: local help, explicit validation, and persistent labels.
5. Density: keep information compact without making it ambiguous.

## Core Rules

- Use text roles, not arbitrary font sizes.
- Keep body text readable by default, usually 16px or larger on web.
- Aim for body line height around 1.45 to 1.65.
- Keep long-form text to roughly 60 to 75 characters per line.
- Keep related labels, inputs, help, and errors close together.
- Use strong contrast for text and meaningful UI boundaries.
- Prefer literal labels and actions over clever copy.
- Never rely on placeholder text as the only label.
- Make the primary action obvious and specific.

## How To Apply It

- For pages with long reading, use larger body text, generous line height, and constrained measure.
- For SaaS dashboards and dense views, preserve clarity with alignment, hierarchy, and readable metadata.
- For forms, keep labels persistent, add local helper text, and show errors next to the field.
- For empty states, buttons, and alerts, say what happened and what the user should do next.
- For review work, check whether the interface can be understood by scanning headings, labels, values, and actions in a few seconds.

## Reference

Use [references/readability-guidelines.md](references/readability-guidelines.md) for the detailed principles, examples, and checklist.
