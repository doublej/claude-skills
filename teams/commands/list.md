---
description: List all team presets (bundled + user overrides) with their one-line descriptions.
allowed-tools: Bash
---

Enumerate available team presets:

```bash
bash ~/.claude/skills/teams/scripts/list-presets.sh
```

Show the output verbatim.
